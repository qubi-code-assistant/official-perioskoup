/**
 * Vitest setup file — runs before each test file
 *
 * Imports @testing-library/jest-dom matchers so assertions like
 * toBeInTheDocument(), toBeVisible(), toHaveAttribute() are available.
 */
import "@testing-library/jest-dom";
